import {connect} from 'react-redux';
import SplashSceneComponent from './SplashSceneComponent';

let mapStateToProps = () => ({});

let mapDispatchToProps = () => ({});

export default connect(mapStateToProps, mapDispatchToProps)(SplashSceneComponent);
